uid = root
gid = root
read only = yes
use chroot = true
max connections = 20
timeout = 180
log format = "%o %h [%a] %m (%u) %f %l"

[volume]
path = /data
auth users = container-user
secrets file = /etc/rsyncd.secrets